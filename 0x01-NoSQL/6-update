// script adds a new attribute to a document in the collection "school"

db.school.find({ name: "Holberton school" }).forEach( doc => {
  db.school.updateOne(
    { _id: doc._id },
    { $set: { address: "972 Mission street" } }
  )
})
